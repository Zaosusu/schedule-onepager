---
name: schedule-onepager
description: 把零散进来的行程信息归档成一张能在邮箱里看的 HTML 排期表，并在被明确要求时发送到使用者自己的邮箱。当使用者提供新的行程信息（群公告截图、活动海报、赛事须知、约谈安排、口头说的日程）、要求查看或修改排期表、说「更新一下排期」「加一条」「发」「发邮箱」时使用。核心是一张唯一的表：只收录已确定日期的事项、一个连续档期一行、每次动表必须重算全部倒计时与已完成状态。适用于同时被多个活动/项目占用档期、需要一眼看清「哪几天被占了」的人。不负责订票订房、不做冲突风险分析、不主动发邮件。另含一个独立的「每日 TODO」模式：只装当天可勾选的动作项，数据与排期表分开、互不合并，使用者说「今日待办」「我的 TODO」「今天做这几件事」时使用。还含一个独立的「每日历史记录」模式：用 SQLite 存每天做了什么（HTML 仅作导出视图），数据与排期表、TODO 都分开，使用者说「今天做了什么」「记录一下」「历史记录」「本周复盘」时使用。
agent_created: true
---

# Schedule Onepager

把零散信息归档成**一张**排期表。**表只回答一个问题：哪几天被占用了。**

## 先读这段

三个设计判断，决定了这个 skill 怎么用：

**1. 一张表，原地覆盖。** 数据文件只有一个（默认 `my/schedule.html`）。不建副本、不建草稿、不建临时文件、不按活动拆分。使用者会用浏览器和邮件客户端两种方式看同一个文件，多一份就会有一份是过期的。

**2. 表要干净，对话可以不干净。** 表里只有六列：日期、星期、活动、身份、事项、倒计时。食宿建议、订票提醒、行前清单、冲突风险分析、待确认事项清单**都不进表**——它们会把「哪几天被占了」这个信号淹掉。但这只约束表格，**不约束对话**：日期临近、信息有缺口、两处说法矛盾、材料有规范、别人家流程里需要配合的环节，都应该在回话里说。

**3. 时间状态会腐烂。** 倒计时、已完成灰底、表头的更新日期，全都随时间失效。所以**任何一次动表都要把全表的时间状态重算一遍**，不能只改被提到的那一行。隔几天再改表时尤其致命：上次写的「3 天」现在可能已经是「已完成」。

## 唯一数据文件

`my/schedule.html`（可在 `config.json` 的 `data_file` 改）。

首次使用：

```bash
mkdir -p my
cp template/schedule.template.html my/schedule.html
cp config.example.json my/config.json     # 然后改 identities / palette
```

`my/` 和 `config.json` 都在 `.gitignore` 里，个人行程不会被提交。

## 硬规则

### 1. 只记录已确定的事

只有**明确日期**的事项才进表。以下不进：

- 宣传材料里的规划意图（例：「后续每月 10 号固定开课」——没定的事不要写进来）
- 只有时段没有确切日期的（例：「10 月上旬决赛」）
- 待通知、待定的环节

拿不准某条是否成立，**先问，不要先写进表**。

### 2. 不做行程顾问

**不要**往表里加：食宿建议、订票提醒、行前准备清单、紧急度评估、冲突风险分析、待确认事项清单、旅途时间预留。

官方安排里本来就有的时间块（如「晚餐 17:30-20:30」）算日程，可以保留；关于食宿的说明文字（餐券规则、协议房价格）不进表，但可以口头说。

见「先读这段」第 2 条：提醒去对话里说，不要挤进表格。

### 3. 与本人无关的事不进表

判断标准：这件事是否需要**使用者本人行动或到场**。例：作为授课老师，「学员报名截止」与本人无关，不进表。

**也不要为了「完整」去补活动内部的细节。** 表的作用是回答「哪几天被占用」，不是活动说明书。默认写到「出差 3 天 / 授课」这个粒度就够，不要自己推算颁证日、开幕式、领导致辞之类的环节——那是编的。

例外：**对方给了精确到分钟的官方安排时**，可以单独做一张逐时段细表。那是拿到的信息，不是推的。

### 4. 身份栏是封闭词表

身份栏的取值必须来自 `config.json` 的 `identities`。

- **不许临时造词。** 同一种身份出现三种写法，表就不能一眼扫完了
- 一件事归不进任何一个词，**问使用者**，不要自己造一个
- 词表里有包含关系时（`identity_notes` 里会写明），**写更高的那个**。写低的等于降级

`config.example.json` 里给了一组示例词表（顾问 ⊃ 评委 / 老师 / 选手 / 主导），那只是示例，按使用者的场景整套替换。

### 5. 不主动发邮件

更新完只做两件事：**口头汇报改动 + 停下。**

**只有**使用者明确说「发」「发邮箱」时才发送。

### 6. 每次动表都要刷新时间状态

任何一次修改都必须顺手把这些**全部**重算：

- **全部倒计时**重算（不是只算新增那行）
- 已经过去的行 → 灰底 `#f2f3f7`、灰字，倒计时列改「已完成」
- 当天的行 → 倒计时列改「今天」
- 表头「更新：YYYY年M月D日（周X）」→ 改成今天
- 标红范围（默认 12 天内）跟着挪

### 7. 看图片不猜

使用者常直接发群公告截图、海报。**看不清、被划掉、被遮挡的文字不要猜**，明确说出哪一处读不出来、请对方补。整行被划掉的内容猜错会污染整张表。

## 收到新信息时的处理流程

**1. 算准星期和倒计时** —— 用脚本算，不要心算：

```bash
python scripts/countdown.py --today 2026-03-09 2026-03-14 2026-03-15
```

**2. 交叉校验年份** —— 公告里的年份经常打错。用**星期**反推：

```bash
python scripts/countdown.py --which-year 08-21
```

公告写「X 年 8 月 21 日（周五）」但那年的 8/21 是周四时，以**星期**为准的可能性更大。发现不一致要明确告知按哪年排的。

**3. 判断是否与本人有关**（规则 3），**确定身份**（规则 4）。

**4. 查冲突** —— 只报「有 / 无冲突」这个事实，不展开分析后果。

**5. 更新 HTML**，同时刷新全表时间状态（规则 6），然后用一两句话说改了什么。**不要发邮件。**

## HTML 约束

这张表要在**邮件客户端**里渲染，所以必须写成老式 HTML：全部内联样式、table 布局，禁 `<style>` 块 / CSS 变量 / flex / grid / `position` / 外部字体，带背景色的 `<tr>` 要同时给 `bgcolor` 属性兜底。

**完整约束和自查清单见 `references/email-html.md`。改 HTML 前读一遍。**

## 表格结构

**主表**（按日期升序，一行 = 一个连续档期）：

| 日期 | 星期 | 活动 | 身份 | 事项 | 倒计时 |
|---|---|---|---|---|---|

- **连续多天合并成一行**（例：「8月25日–8月29日」一行，内部细分写在「事项」列：报到 8/25-26、决赛 8/27-29）。不要拆成 5 行
- **每个活动一种底色**，同一活动所有行同色。新活动从 `config.json` 的 `palette` 取下一组未占用的色，并**同步更新图例**
- 周末的星期用红色 `#c9302c`；`highlight_days`（默认 12）天内的倒计时红色加粗
- 已过去的行统一灰底 `#f2f3f7`，活动色让位给灰色

**主表之后**：

- **图例**：3 列网格，浅灰底框起来，不要挤成一行
- **身份说明**：把封闭词表的定义印在表上
- **逐时段细表**（可选）：只在有官方逐时段安排时做
- **页脚**：随手要查的参考信息

骨架见 `template/schedule.template.html`，填满的样子见 `examples/schedule-demo.html`。

## 发送邮件

**仅在使用者明确说「发」时执行。**

```bash
SMTP_USER='you@example.com' SMTP_PASS='<向使用者索取>' \
  python scripts/send_schedule.py my/schedule.html
```

- 凭据**只从环境变量走**。**授权码不要存盘**——不写进 config、不写进脚本、不写进任何文件。每次向使用者索取；如果对话里已经给过就直接用，并说明「没写进文件、只在这次发送时用」
- 收件人默认等于登录账号（发给自己）。QQ / 163 强制发件人 = 登录账号
- **只发 HTML 正文，不附加 HTML 附件**——手机上打不开附件，纯属占空间
- 不确定参数对不对，先 `--dry-run`
- 各家邮箱怎么拿授权码、报错怎么对照，见 `references/smtp-setup.md`

## 每日 TODO 模式（与排期表独立）

排期表回答「哪几天被占用了」；TODO 回答「今天要动手做什么」。两者**独立成两个文件、互不读取、互不合并**：

- 排期表 = `my/schedule.html`：按日期记已确定的承诺（赛事、出差、答辩…）
- TODO = `my/todo.html`：只装**当天**可勾选的动作项

使用者明确要过「TODO 不要跟排期表混在一起」，所以这条是硬边界：

- **不要**从 `my/schedule.html` 自动搬运条目进 TODO，也**不要**在 TODO 里复述赛事 / 排期
- 使用者在对话里口述今天的任务，AI 逐条写进 TODO
- TODO 的行就三列：状态 / 任务 / 备注

### TODO 的规则

1. **只装当天。** 今天的待办归今天，明天的事不预先塞进来。每天首次动表把表头日期改成今天，并把昨天没划掉、已过期的项标灰（保留文字，不自动删除）。
2. **状态三档固定**：`待办`（红 `#c9302c` 加粗）/ `进行中`（蓝 `#1f3fa8` 加粗）/ `已完成`（整行灰字 `#7d8399`，任务加删除线 `<s>`）。不许现造第四档。
3. **不做顾问。** 和排期表同理：食宿、订票、行前清单、紧急度评估不进表，留在对话里说。
4. **不主动发邮件。** 同排期表：只有使用者说「发」才发。TODO 沿用 `scripts/send_schedule.py my/todo.html`（同一套邮件 HTML 约束，见 `references/email-html.md`）。
5. **脱敏。** TODO 是个人隐私，只放 `my/`（已 gitignore），仓库里不出现任何真实任务。

骨架见 `template/todo.template.html`，填满的样子见 `examples/todo-demo.html`：

```bash
cp template/todo.template.html my/todo.html
```

## 每日历史记录模式（SQLite，与排期表 / TODO 独立）

记「每天做了什么」的流水账，给日后复盘用。它和排期表（未来承诺）、TODO（当天动作）**完全分开**，互不读取：

- 排期表 = `my/schedule.html`：已确定的承诺
- TODO = `my/todo.html`：今天要动手的
- 历史记录 = `daily_history.db`（SQLite）：每天结束记一笔「做了什么」

### 为什么是数据库而不是 HTML

历史记录会一直累加，手写 HTML 既容易漏改又会越改越乱。所以**数据源是 SQLite**，HTML 只是可选的浏览视图（`history.py export` 现生成）。任何一次追加都只走命令，不在 HTML 上手改。

### 用法

```bash
# 追加（--date 省略默认今天；--cat 分类可选）
python scripts/history.py add --date 2026-08-18 --cat 评审细则 --text "决赛评审细则 v2 已提交"

# 查询（倒序，可按日期过滤）
python scripts/history.py list [--date 2026-08-18] [--limit 20]

# 统计（按日期+分类）
python scripts/history.py stats

# 导出 HTML 视图（数据源仍是 DB）
python scripts/history.py export [--out 每日历史记录.html]
```

### 规则

1. **只记事实，不记情绪。** 一条就是「日期 + 分类 + 做了什么」。分类是可复用的词（如 鼓楼黑客松 / 评审细则 / 微信skill），用于日后 `stats` 复盘。
2. **每天结束追加一次**，不要攒一周再补——细节会忘。
3. **私隐。** `daily_history.db` 是个人数据，只在 `my/`（已 gitignore）或你自己的工作目录里，仓库不提交任何真实记录。
4. **与 TODO 不合并。** TODO 是「待办」，历史记录是「已办」，两件事。不要在历史记录里复述 TODO，也不要从 TODO 自动搬。
5. **DB 路径多源解析**：`scripts/history.py` 按 `--db` > 环境变量 `DAILY_HISTORY_DB` > 脚本同目录的 `daily_history.db` > `<skill根>/my/daily_history.db` 顺序找库；把自己 cp 到工作目录用时，自动命中同级的库。

脱敏示例见 `examples/history-example.md`。

## 沟通风格

- 汇报改动用一两句话，不要复述整张表，除非被要求「展示一下」
- 信息缺失就直接问，不要列举各种假设情况推演
- 使用者自己写错或说反了（例：把 9 月说成 8 月）会直接更正，跟着改就行，不用追问
- 可以也应该给出善意、有用的提醒——但提醒留在对话里，别进表
